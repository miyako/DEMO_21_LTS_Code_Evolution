// InputType onChange
// Update the form based on the new input type

// Call the updateType method of the contact detail form
Form:C1466.updateType()

// Call the createDynamicFields method to create fields dynamically based on new type
Form:C1466.createDynamicFields()
